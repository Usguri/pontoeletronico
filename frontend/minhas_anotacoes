projeto 1 https://github.com/ABKADRI-AMINE/Employee-Time-Tracking-Application-Spring-Boot-Angular-Docker_finalV/tree/main
projeto 2 https://github.com/Brunomtk/Ponto-eletronico?utm_source=chatgpt.com

# Criar diferentes tipos de projeto

dotnet new console -n MeuApp # Console app
dotnet new webapi -n MinhaAPI # Web API
dotnet new mvc -n MeuSiteMVC # MVC Web App
dotnet new classlib -n MinhaLib # Class Library

# Gerenciar packages

dotnet add package NomeDoPacote # Adicionar pacote
dotnet remove package NomeDoPacote # Remover pacote
dotnet list package # Listar pacotes

# Build e execução

dotnet build # Compilar
dotnet run # Executar
dotnet publish # Publicar
dotnet test # Executar testes

MeuProjeto/
├── Controllers/ # Endpoints da API (POST/GET)
├── Models/ # Classes de dados/entidades
├── DTOs/ # Objetos de transferência de dados
├── Data/ # Context do banco de dados
├── Services/ # Lógica de negócio
├── Program.cs # Configuração e inicialização
├── appsettings.json # Configurações da aplicação
└── MeuProjeto.csproj # Dependências do projeto

instalei
dotnet dev-certs https --trust
dotnet add package Microsoft.EntityFrameworkCore --version 6.0.33
dotnet add package Microsoft.EntityFrameworkCore.Tools --version 6.0.33
dotnet add package Pomelo.EntityFrameworkCore.MySql --version 6.0.2
dotnet tool install --global dotnet-ef --version 6.0.33

precisei atualizar para versão 6 do dotnet para poder utilizar o mysql
configurar o banco de dados em appsettings
dotnet ef migrations add tabela -> criar as migrattions
dotnet ef database update - > atualizar

acessar o swagger http://localhost:5034/swagger/index.html
